/**
 * Sends a prompt to Groq and returns the raw text response.
 * Used by every tool that needs an LLM call.
 */
export declare function askGroq(systemPrompt: string, userPrompt: string): Promise<string>;
//# sourceMappingURL=groqClient.d.ts.map